# RAG Quality Evaluation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a reproducible assignment package that shows EDDR's RAG baseline, 10-question evaluation set, at least two retrieval/query improvement experiments, and a final Korean analysis report.

**Architecture:** Reuse EDDR's existing retrieval assets instead of creating a parallel RAG stack: `docs/rag_quality/questions.yaml` supplies the assignment question set, `scripts/bench_retrieval.py` runs controlled retrieval experiments, `eddr golden --golden-set ...` runs the end-to-end search-path check, and a new pure report module converts those artifacts into `reports/rag_quality/ASSIGNMENT_REPORT.md`. The generated report treats EDDR's photo caption as the retrieval document unit, because the current system does not chunk long text documents.

**Tech Stack:** Python 3.12, PyYAML, existing SQLite ledger + Chroma sidecar, `gemma4:e2b` query extractor, `qwen3-embedding:8b`, existing `QueryService`, pytest, `uv`.

---

## Context And Constraints

- EDDR's current query flow is local search, not external-LLM answer generation: `POST /api/search` calls `QueryExtractor`, resolves place-derived `trip_ids`, calls `QueryService.semantic_search_photos`, and groups results by KST date lane (`docs/PLAN.md:188-205`, `src/eddr/server/routes/search.py:91-115`).
- The vector store decision is fixed: SQLite is the canonical ledger, and Chroma is the persistent caption-text vector sidecar (`docs/adr/0006-vector-store-selection.md:21-28`, `wiki/decisions/vector-store.md:12-19`).
- Current retrieval levers already exist: query instruction, adaptive over-fetch, optional keyword RRF, optional reranker, and benchmark output to JSONL/Markdown (`src/eddr/query/tools.py:19-35`, `scripts/bench_retrieval.py:236-326`, `scripts/bench_retrieval.py:419-442`).
- The existing golden-set runner can load an alternate YAML path and evaluate `match` rules without changing production code (`src/eddr/cli.py:178-199`, `src/eddr/query/golden.py:128-146`, `src/eddr/query/golden.py:289-362`).
- Use `UV_CACHE_DIR=/private/tmp/uv-cache-eddr` for `uv run` verification in this repo.

## File Structure

| File | Action | Responsibility |
|---|---|---|
| `docs/rag_quality/questions.yaml` | Create | Assignment question set: 10 user-selected questions, difficulty/category labels, expected signal, and machine-readable match rules compatible with `eddr golden`. |
| `src/eddr/eval/__init__.py` | Create | Package marker for evaluation/report utilities. |
| `src/eddr/eval/rag_quality.py` | Create | Pure functions to validate questions, parse retrieval experiment JSONL, compute deltas, and render the assignment report. |
| `scripts/build_rag_quality_report.py` | Create | Thin CLI wrapper that reads question/experiment artifacts and writes `reports/rag_quality/ASSIGNMENT_REPORT.md`. |
| `tests/eval/test_rag_quality.py` | Create | Unit tests for validation, JSONL parsing, delta computation, and required report sections. |
| `reports/rag_quality/retrieval/experiments.jsonl` | Runtime create | Experiment ledger generated by `scripts/bench_retrieval.py`. |
| `reports/rag_quality/retrieval/RESULTS.md` | Runtime create | Retrieval comparison table generated by `scripts/bench_retrieval.py`. |
| `reports/rag_quality/golden/*.md` | Runtime create | Search-path report generated by `eddr golden`. |
| `reports/rag_quality/ASSIGNMENT_REPORT.md` | Runtime create | Final submission report. |

## Experiment Design

Baseline for the assignment:

- Document unit: one photo caption plus parsed keywords. There is no `chunk_size`/`chunk_overlap`; the "chunk" is one caption document. This should be stated explicitly because the assignment examples assume text documents.
- Embedding model: `qwen3-embedding:8b`.
- Vector store: Chroma `eddr_caption_text_v1`.
- Metadata ledger: SQLite `data/eddr.sqlite`.
- Retriever: top-k benchmark at `k=20`, vector retrieval through `QueryService.semantic_search_photos`.
- Query extraction: `gemma4:e2b`, structured output, temperature 0.
- Production search cap: `/api/search` uses `_SEARCH_K = 50`; experiment cap starts at `k=20` for comparability.

Required improvement experiments:

1. **Query embedding prompt/instruction**: compare raw query embedding (`--instruct none`) with qwen3 recommended instruction (default).
2. **Top-k change**: compare `k=20` with `k=50`.
3. **Retriever fusion**: compare vector-only with BM25 keyword RRF (`--rrf`). This is an extra experiment beyond the minimum two.

Optional experiment:

- **Reranker**: `--rerank`, only if the local environment already has the ad-hoc sentence-transformers dependency available. Do not add it to `pyproject.toml` for this assignment unless the user explicitly wants production reranking.

---

### Task 1: Create Assignment Question Set

**Files:**
- Create: `docs/rag_quality/questions.yaml`

- [ ] **Step 1: Create the directory**

Run:

```bash
mkdir -p docs/rag_quality
```

Expected: command exits 0 and `docs/rag_quality/` exists.

- [ ] **Step 2: Write the question YAML**

Create `docs/rag_quality/questions.yaml` with exactly this content:

```yaml
# EDDR RAG quality evaluation questions.
# Source: user-selected docs/golden_set.yaml questions, re-shaped for the RAG quality assignment.
# The extra fields are ignored by eddr golden; match is consumed by src/eddr/query/golden.py.
version: 2
assignment: rag_quality_evaluation
selected_by: user
selected_at: "2026-06-11"
pass_threshold: 8

questions:
  - id: G01
    category: multi_document_semantic
    assignment_type: 여러 문서를 종합해야 하는 질문
    difficulty: medium
    origin: Q001
    question: "이탈리아 여행 사진 중 돌로미티나 산악 풍경을 찾아줘"
    answer_type: photo_list
    expected_signal: "이탈리아 trip 범위 안에서 mountain, alpine, peak 성격의 캡션 사진이 상위에 노출되어야 한다."
    expect: "이탈리아 trip 기간 사진 위주로 산악 풍경이 상위에 와야 한다."
    reference:
      trip: "trip_20190629_01 · 2019-06-29~07-12"
      caption_pool: "trip 내 mountain/alpine/peak 캡션 104장"
    match:
      caption_contains_any:
        words: ["mountain", "alpine", "peak"]
        top_k: 20

  - id: G02
    category: multi_document_semantic
    assignment_type: 여러 문서를 종합해야 하는 질문
    difficulty: hard
    origin: Q004
    question: "아이슬란드 여행에서 차량 이동이나 도로가 나온 사진을 찾아줘"
    answer_type: photo_list
    expected_signal: "아이슬란드 여러 trip에서 road, car, vehicle, driving 장면을 넓게 회수해야 한다."
    expect: "아이슬란드 trip 사진 중 도로·차량 장면이 상위에 와야 한다."
    reference:
      trips: "trip_20210218_01 · trip_20220917_01 · trip_20250924_01"
      caption_pool: "아이슬란드 trip 내 road/car/vehicle/driving 캡션 130장"
    match:
      caption_contains_any:
        words: ["road", "car", "vehicle", "driving"]
        top_k: 20

  - id: G03
    category: conditional_location_semantic
    assignment_type: 조건 비교가 필요한 질문
    difficulty: hard
    origin: Q005
    question: "몽골에서 별이나 은하수가 나온 사진 찾아줘"
    answer_type: photo_list
    expected_signal: "은하수 사진 중 국내 운여해변 distractor를 피하고 몽골 trip 결과를 우선해야 한다."
    expect: "몽골 trip 기간의 밤하늘·은하수 사진이 상위에 와야 한다."
    reference:
      trip: "trip_20180713_01 · 2018-07-13~21"
      caption_pool: "trip 내 milky way/starry/night sky/stars 캡션 19장"
      distractor: "운여해변 2015-12 은하수 19장"
    match:
      caption_contains_any:
        words: ["milky way", "starry", "night sky", "stars"]
        top_k: 20

  - id: G04
    category: simple_fact
    assignment_type: 단순 사실 조회
    difficulty: medium
    origin: Q009
    question: "부산 1박2일 여행 사진은 언제 찍힌 것들이야?"
    answer_type: fact
    expected_signal: "부산 1박2일 trip의 날짜 lane이 상위에 노출되어야 한다."
    expect: "2018-12-29~30 또는 2025-04-04~05 부산 trip 날짜가 확인되어야 한다."
    reference:
      trips: "trip_20181229_01(2018-12-29~30) · trip_20250404_01(2025-04-04~05)"
    match:
      date_lane_top:
        date: "2018-12-29"
        within: 3

  - id: G05
    category: natural_language_semantic
    assignment_type: 사용자가 실제로 물어볼 법한 자연어 질문
    difficulty: medium
    origin: Q010
    question: "겹벚꽃이나 봄꽃이 크게 나온 사진 찾아줘"
    answer_type: photo_list
    expected_signal: "꽃 클로즈업 사진을 찾되 특정 연도를 과추출하지 않아야 한다."
    expect: "봄꽃이 크게 나온 사진이 상위에 와야 한다."
    reference:
      folders: "20190501_철산 · 20190505_개심사"
      caption_pool: "blossom/flower/cherry 캡션 21장"
    match:
      caption_contains_any:
        words: ["blossom", "flower", "cherry"]
        top_k: 20

  - id: G06
    category: data_gap_semantic
    assignment_type: 여러 문서를 종합해야 하는 질문
    difficulty: hard
    origin: Q025
    question: "개심사에서 절 건물과 꽃이 함께 나온 사진 찾아줘"
    answer_type: photo_list
    expected_signal: "GPS와 지명 캡션이 부족한 데이터 부재 케이스에서 temple/flower 후보를 찾아야 한다."
    expect: "개심사 폴더 사진이 포함되거나, 못 찾을 때 데이터 한계를 드러내야 한다."
    reference:
      folder: "20190505_개심사 16장 · GPS 0"
    match:
      caption_contains_any:
        words: ["temple", "flower", "flowers"]
        top_k: 20

  - id: G07
    category: conditional_location_semantic
    assignment_type: 조건 비교가 필요한 질문
    difficulty: medium
    origin: Q031
    question: "제주에서 검은 바위 해변이나 현무암 해안이 나온 사진 찾아줘"
    answer_type: photo_list
    expected_signal: "제주 장소 조건과 basalt/black rock 장면 조건을 함께 만족해야 한다."
    expect: "제주시·서귀포시 사진 중 검은 바위·현무암 해안 장면이 상위에 와야 한다."
    reference:
      caption_pool: "제주/서귀포 geocode + volcanic/basalt/black rock/rocky 캡션 82장"
    match:
      caption_contains_any:
        words: ["volcanic", "basalt", "black rock", "rocky"]
        top_k: 20

  - id: G08
    category: simple_fact
    assignment_type: 단순 사실 조회
    difficulty: easy
    origin: user-new
    question: "내가 이탈리아를 언제 갔더라?"
    answer_type: fact
    expected_signal: "이탈리아 trip 시작 날짜 lane이 상위에 노출되어야 한다."
    expect: "2019년 6월 말~7월 초를 제시하면 통과."
    reference:
      trip: "trip_20190629_01 · 2019-06-29~07-12"
    match:
      date_lane_top:
        date: "2019-06-29"
        within: 3

  - id: G09
    category: natural_language_semantic
    assignment_type: 사용자가 실제로 물어볼 법한 자연어 질문
    difficulty: hard
    origin: user-new
    question: "용산에서 뭘 먹었었는지 보여줘"
    answer_type: photo_list
    expected_signal: "서울 용산구 음식 사진을 찾고 평창 용산리 동음이의 잡음을 줄여야 한다."
    expect: "용산구 음식 사진이 상위에 와야 한다."
    reference:
      pool: "서울 용산구 food/meal/restaurant/dish 캡션 27장"
      homonym: "평창 대관령면 용산리 18장"
    match:
      caption_contains_any:
        words: ["food", "meal", "restaurant", "dish"]
        top_k: 20

  - id: G10
    category: broad_semantic
    assignment_type: 사용자가 실제로 물어볼 법한 자연어 질문
    difficulty: easy
    origin: user-new
    question: "은하수 사진들 찾아줘"
    answer_type: photo_list
    expected_signal: "장소 조건 없이 전체 라이브러리에서 milky way 사진을 폭넓게 회수해야 한다."
    expect: "은하수 사진이 상위에 넓게 반환되어야 한다."
    reference:
      caption_pool: "milky way 캡션 36장"
      no_gps: "36장 중 34장 GPS 없음"
    match:
      caption_contains_any:
        words: ["milky way"]
        top_k: 20
```

- [ ] **Step 3: Validate the YAML with the existing golden loader**

Run:

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run python -c "from pathlib import Path; from eddr.query.golden import load_golden_set; qs=load_golden_set(Path('docs/rag_quality/questions.yaml')); print(len(qs), qs[0].id, qs[-1].id)"
```

Expected output:

```text
10 G01 G10
```

- [ ] **Step 4: Commit the question set**

Run:

```bash
git add docs/rag_quality/questions.yaml
git commit -m "docs: add rag quality evaluation question set"
```

Expected: commit succeeds with one tracked file.

---

### Task 2: Add Report Utility Tests

**Files:**
- Create: `tests/eval/test_rag_quality.py`
- Create: `src/eddr/eval/__init__.py`
- Create: `src/eddr/eval/rag_quality.py`

- [ ] **Step 1: Create package and test directories**

Run:

```bash
mkdir -p src/eddr/eval tests/eval
touch src/eddr/eval/__init__.py
```

Expected: directories and empty package marker exist.

- [ ] **Step 2: Write a failing test file**

Create `tests/eval/test_rag_quality.py`:

```python
"""RAG quality report utilities."""

from __future__ import annotations

import json
from pathlib import Path

import pytest

from eddr.eval.rag_quality import (
    ExperimentRecord,
    load_experiment_records,
    load_question_specs,
    render_assignment_report,
    summarize_experiments,
)


QUESTIONS_YAML = """\
version: 2
questions:
  - id: G01
    question: "이탈리아 산악 풍경"
    category: multi_document_semantic
    assignment_type: 여러 문서를 종합해야 하는 질문
    difficulty: medium
    answer_type: photo_list
    expected_signal: "산악 풍경"
    match:
      caption_contains_any:
        words: ["mountain"]
        top_k: 20
  - id: G02
    question: "내가 이탈리아를 언제 갔더라?"
    category: simple_fact
    assignment_type: 단순 사실 조회
    difficulty: easy
    answer_type: fact
    expected_signal: "날짜 lane"
    match:
      date_lane_top:
        date: "2019-06-29"
        within: 3
"""


def _record(label: str, mean: float, latency: float = 4.2) -> dict:
    return {
        "ts": "2026-06-14 16:00:00",
        "git_rev": "abc1234",
        "label": label,
        "config": {
            "k": 20,
            "embed_model": "qwen3-embedding:8b",
            "instruct": None if "raw" in label else "Instruct: Given a web search query\\nQuery:{query}",
            "rrf": "rrf" in label,
            "rerank": False,
        },
        "questions": [
            {
                "qid": "G01",
                "gt_size": 10,
                "returned": 20,
                "hits": 8,
                "recall@k": 0.8,
                "recall_norm": mean,
                "precision": 0.4,
                "latency_s": latency,
                "diag_recall@100": 0.5,
                "diag_recall@500": 0.7,
                "diag_recall@2000": 1.0,
            }
        ],
        "aggregate": {
            "mean_recall@k": mean,
            "mean_recall_norm": mean,
            "mean_diag_recall@500": 0.7,
            "mean_returned": 20.0,
            "total_latency_s": latency,
        },
    }


def test_load_question_specs_keeps_assignment_metadata(tmp_path: Path):
    path = tmp_path / "questions.yaml"
    path.write_text(QUESTIONS_YAML, encoding="utf-8")

    questions = load_question_specs(path)

    assert [q.id for q in questions] == ["G01", "G02"]
    assert questions[0].assignment_type == "여러 문서를 종합해야 하는 질문"
    assert questions[1].difficulty == "easy"
    assert questions[1].match["date_lane_top"]["date"] == "2019-06-29"


def test_load_question_specs_rejects_missing_required_fields(tmp_path: Path):
    path = tmp_path / "bad.yaml"
    path.write_text("version: 2\nquestions:\n  - id: G01\n", encoding="utf-8")

    with pytest.raises(ValueError, match="question"):
        load_question_specs(path)


def test_load_experiment_records_reads_jsonl(tmp_path: Path):
    path = tmp_path / "experiments.jsonl"
    path.write_text(
        json.dumps(_record("baseline-raw-k20"), ensure_ascii=False)
        + "\n"
        + json.dumps(_record("exp1-instruct-default-k20", 0.9), ensure_ascii=False)
        + "\n",
        encoding="utf-8",
    )

    records = load_experiment_records(path)

    assert [r.label for r in records] == ["baseline-raw-k20", "exp1-instruct-default-k20"]
    assert records[1].mean_recall_norm == 0.9
    assert records[0].config["k"] == 20


def test_summarize_experiments_computes_delta_against_baseline():
    records = [
        ExperimentRecord.from_json(_record("baseline-raw-k20", 0.5)),
        ExperimentRecord.from_json(_record("exp1-instruct-default-k20", 0.65)),
        ExperimentRecord.from_json(_record("exp2-topk50", 0.75, latency=8.0)),
    ]

    rows = summarize_experiments(records, baseline_label="baseline-raw-k20")

    assert rows[0]["delta"] == 0.0
    assert rows[1]["delta"] == 0.15
    assert rows[2]["total_latency_s"] == 8.0


def test_render_assignment_report_contains_required_sections(tmp_path: Path):
    qpath = tmp_path / "questions.yaml"
    qpath.write_text(QUESTIONS_YAML, encoding="utf-8")
    questions = load_question_specs(qpath)
    records = [
        ExperimentRecord.from_json(_record("baseline-raw-k20", 0.5)),
        ExperimentRecord.from_json(_record("exp1-instruct-default-k20", 0.65)),
    ]

    report = render_assignment_report(
        questions=questions,
        records=records,
        baseline_label="baseline-raw-k20",
        golden_report_path=Path("reports/rag_quality/golden/example.md"),
    )

    assert "# EDDR RAG 품질 평가 및 개선 리포트" in report
    assert "## 1. 평가용 질문셋" in report
    assert "## 2. Baseline RAG 설정" in report
    assert "## 3. 성능 개선 실험" in report
    assert "## 4. 결과 분석" in report
    assert "baseline-raw-k20" in report
    assert "exp1-instruct-default-k20" in report
```

- [ ] **Step 3: Run the test and confirm it fails because the module is empty**

Run:

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run pytest tests/eval/test_rag_quality.py -q
```

Expected: FAIL with import errors such as:

```text
ImportError: cannot import name 'ExperimentRecord' from 'eddr.eval.rag_quality'
```

---

### Task 3: Implement Report Utilities

**Files:**
- Modify: `src/eddr/eval/rag_quality.py`
- Test: `tests/eval/test_rag_quality.py`

- [ ] **Step 1: Write the implementation**

Replace `src/eddr/eval/rag_quality.py` with:

```python
"""Utilities for the EDDR RAG quality assignment report."""

from __future__ import annotations

import json
from dataclasses import dataclass
from pathlib import Path
from typing import Any

import yaml


REQUIRED_QUESTION_FIELDS = (
    "id",
    "question",
    "category",
    "assignment_type",
    "difficulty",
    "answer_type",
    "expected_signal",
    "match",
)

BASELINE_CONFIG = {
    "document_unit": "photo caption document: one caption body plus search keywords per photo",
    "chunk_size": "not applicable; EDDR does not split long documents because each photo caption is already one short retrieval unit",
    "chunk_overlap": "not applicable",
    "embedding_model": "qwen3-embedding:8b",
    "vector_store": "Chroma persistent sidecar at data/index/chroma, collection eddr_caption_text_v1",
    "metadata_store": "SQLite ledger at data/eddr.sqlite",
    "retriever": "QueryService.semantic_search_photos, benchmark k=20, production search cap k=50",
    "query_extractor": "gemma4:e2b via Ollama structured output, temperature 0",
    "prompt_or_instruction": "baseline label controls whether qwen3 query instruction is disabled or enabled",
    "llm": "runtime external LLM is not used; local gemma4:e2b only extracts query structure",
}

EXPERIMENT_NOTES = {
    "baseline-raw-k20": {
        "changed": "질의 임베딩 instruction을 끄고 k=20으로 검색한다.",
        "expected": "가장 단순한 벡터 검색 기준선을 만든다.",
        "why": "문서 단위가 짧은 사진 캡션이라 raw query도 어느 정도 동작하지만, qwen3 계열은 질의 측 instruction이 있을 때 retrieval 정렬이 안정적일 수 있다.",
    },
    "exp1-instruct-default-k20": {
        "changed": "qwen3 권장 query instruction을 켠다.",
        "expected": "한국어 질의를 검색 질의로 더 명확히 임베딩해 semantic recall이 올라간다.",
        "why": "문서 쪽은 raw caption이고 질의 쪽만 검색 지시문을 붙이면 query/document 역할 구분이 선명해진다.",
    },
    "exp2-topk50": {
        "changed": "반환 상한을 k=20에서 k=50으로 늘린다.",
        "expected": "recall은 올라가고 precision과 latency는 나빠질 수 있다.",
        "why": "사진 검색 UI에서는 더 많은 후보가 날짜 lane에 흩어져 나타나므로, 상위 결과 품질과 탐색 폭 사이의 트레이드오프가 생긴다.",
    },
    "exp3-rrf-k20": {
        "changed": "QueryExtractor가 만든 영어 keywords를 FTS5 BM25 검색에 넣고 RRF로 vector leg와 융합한다.",
        "expected": "road, food, milky way처럼 캡션에 직접 등장하는 단어는 보강되지만, temple, flower처럼 넓은 단어는 잡음을 만들 수 있다.",
        "why": "RRF는 여러 rank list를 합산하는 방식이라 정확한 lexical hit가 있을 때 강하지만, 광범위 키워드는 관련 없는 사진도 높은 점수로 끌어올린다.",
    },
    "exp4-rerank-k20": {
        "changed": "상위 후보를 cross-encoder reranker로 재정렬한다.",
        "expected": "정밀도가 올라갈 수 있지만 한국어 질의와 영어 캡션 조합에서는 손해가 날 수 있다.",
        "why": "reranker는 후보 생성 실패를 고칠 수 없고, ko-en pair 점수가 약하면 기존 embedding 순위보다 나쁜 재정렬을 만들 수 있다.",
    },
}


@dataclass(frozen=True)
class QuestionSpec:
    """One assignment evaluation question."""

    id: str
    question: str
    category: str
    assignment_type: str
    difficulty: str
    answer_type: str
    expected_signal: str
    match: dict[str, Any]


@dataclass(frozen=True)
class ExperimentRecord:
    """One row from scripts/bench_retrieval.py experiments.jsonl."""

    label: str
    ts: str
    git_rev: str
    config: dict[str, Any]
    questions: list[dict[str, Any]]
    aggregate: dict[str, Any]

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "ExperimentRecord":
        """Build a typed record from one JSON object."""
        return cls(
            label=str(data["label"]),
            ts=str(data.get("ts", "")),
            git_rev=str(data.get("git_rev", "unknown")),
            config=dict(data.get("config") or {}),
            questions=list(data.get("questions") or []),
            aggregate=dict(data.get("aggregate") or {}),
        )

    @property
    def mean_recall_norm(self) -> float:
        """Mean normalized recall across benchmark questions."""
        return float(self.aggregate.get("mean_recall_norm", 0.0))

    @property
    def mean_diag_recall_500(self) -> float:
        """Mean global embedding-leg recall@500."""
        return float(self.aggregate.get("mean_diag_recall@500", 0.0))

    @property
    def total_latency_s(self) -> float:
        """Total benchmark latency in seconds."""
        return float(self.aggregate.get("total_latency_s", 0.0))


def load_question_specs(path: Path) -> list[QuestionSpec]:
    """Load and validate assignment question metadata."""
    data = yaml.safe_load(path.read_text(encoding="utf-8"))
    raw_questions = data.get("questions") if isinstance(data, dict) else None
    if not isinstance(raw_questions, list) or not raw_questions:
        raise ValueError(f"{path}: questions must be a non-empty list")

    questions: list[QuestionSpec] = []
    for index, raw in enumerate(raw_questions, start=1):
        if not isinstance(raw, dict):
            raise ValueError(f"{path}: question #{index} must be a mapping")
        for field in REQUIRED_QUESTION_FIELDS:
            if field not in raw:
                raise ValueError(f"{path}: question #{index} missing required field {field!r}")
        questions.append(
            QuestionSpec(
                id=str(raw["id"]),
                question=str(raw["question"]),
                category=str(raw["category"]),
                assignment_type=str(raw["assignment_type"]),
                difficulty=str(raw["difficulty"]),
                answer_type=str(raw["answer_type"]),
                expected_signal=str(raw["expected_signal"]),
                match=dict(raw["match"] or {}),
            )
        )
    return questions


def load_experiment_records(path: Path) -> list[ExperimentRecord]:
    """Read benchmark JSONL records in append order."""
    records: list[ExperimentRecord] = []
    for line_number, line in enumerate(path.read_text(encoding="utf-8").splitlines(), start=1):
        if not line.strip():
            continue
        try:
            records.append(ExperimentRecord.from_json(json.loads(line)))
        except json.JSONDecodeError as exc:
            raise ValueError(f"{path}:{line_number}: invalid JSON") from exc
    if not records:
        raise ValueError(f"{path}: no experiment records")
    return records


def summarize_experiments(
    records: list[ExperimentRecord], baseline_label: str
) -> list[dict[str, Any]]:
    """Create comparison rows with deltas against the named baseline."""
    baseline = next((record for record in records if record.label == baseline_label), None)
    if baseline is None:
        labels = ", ".join(record.label for record in records)
        raise ValueError(f"baseline label {baseline_label!r} not found in records: {labels}")
    base_value = baseline.mean_recall_norm
    rows: list[dict[str, Any]] = []
    for record in records:
        rows.append(
            {
                "label": record.label,
                "k": record.config.get("k"),
                "mean_recall_norm": record.mean_recall_norm,
                "delta": round(record.mean_recall_norm - base_value, 3),
                "mean_diag_recall@500": record.mean_diag_recall_500,
                "total_latency_s": record.total_latency_s,
                "rrf": bool(record.config.get("rrf")),
                "rerank": bool(record.config.get("rerank")),
            }
        )
    return rows


def render_assignment_report(
    *,
    questions: list[QuestionSpec],
    records: list[ExperimentRecord],
    baseline_label: str,
    golden_report_path: Path | None = None,
) -> str:
    """Render the Korean assignment report from question and experiment artifacts."""
    rows = summarize_experiments(records, baseline_label)
    lines: list[str] = [
        "# EDDR RAG 품질 평가 및 개선 리포트",
        "",
        "## 결론",
        "",
        _render_conclusion(rows),
        "",
        "## 1. 평가용 질문셋",
        "",
        f"- 총 문항 수: {len(questions)}",
        "- 문항 구성: 단순 사실 조회, 여러 문서 종합, 조건 비교, 실제 사용 자연어 질문을 포함한다.",
        "",
        "| ID | 난이도 | 유형 | 질문 | 평가 신호 |",
        "|---|---|---|---|---|",
    ]
    for question in questions:
        lines.append(
            f"| {question.id} | {question.difficulty} | {question.assignment_type} | "
            f"{question.question} | {question.expected_signal} |"
        )

    lines += [
        "",
        "## 2. Baseline RAG 설정",
        "",
        "| 항목 | 설정 |",
        "|---|---|",
    ]
    for key, value in BASELINE_CONFIG.items():
        lines.append(f"| {key} | {value} |")

    lines += [
        "",
        "## 3. 성능 개선 실험",
        "",
        "| label | k | RRF | rerank | mean recall norm | baseline 대비 | diag recall@500 | latency(s) |",
        "|---|---:|---|---|---:|---:|---:|---:|",
    ]
    for row in rows:
        lines.append(
            f"| {row['label']} | {row['k']} | {row['rrf']} | {row['rerank']} | "
            f"{row['mean_recall_norm']:.3f} | {row['delta']:+.3f} | "
            f"{row['mean_diag_recall@500']:.3f} | {row['total_latency_s']:.1f} |"
        )

    lines += [
        "",
        "## 4. 결과 분석",
        "",
    ]
    for row in rows:
        note = _note_for_label(str(row["label"]))
        lines += [
            f"### {row['label']}",
            "",
            f"- 변경한 설정: {note['changed']}",
            f"- 기대 효과: {note['expected']}",
            f"- 실제 결과: mean recall norm {row['mean_recall_norm']:.3f}, "
            f"baseline 대비 {row['delta']:+.3f}, latency {row['total_latency_s']:.1f}s.",
            f"- 해석: {note['why']}",
            "",
        ]

    lines += [
        "## 5. 검증 방법",
        "",
        "- 질문셋 YAML은 `eddr.query.golden.load_golden_set`으로 로드 가능해야 한다.",
        "- retrieval 실험은 `scripts/bench_retrieval.py`가 생성한 JSONL 원장을 기준으로 비교한다.",
        "- end-to-end 검색 검증은 `eddr golden --golden-set docs/rag_quality/questions.yaml`로 수행한다.",
    ]
    if golden_report_path is not None:
        lines.append(f"- 이번 리포트와 연결된 golden report: `{golden_report_path}`")
    lines.append("")
    return "\n".join(lines)


def _render_conclusion(rows: list[dict[str, Any]]) -> str:
    best = max(rows, key=lambda row: row["mean_recall_norm"])
    baseline = rows[0]
    return (
        f"가장 좋은 실험은 `{best['label']}`이며 mean recall norm "
        f"{best['mean_recall_norm']:.3f}를 기록했다. 기준선 `{baseline['label']}` 대비 "
        f"{best['delta']:+.3f} 차이다. 이 값은 캡션 LIKE 기반 proxy ground truth에 대한 "
        "상대 비교이므로, 최종 판단은 golden report의 lane/상위 사진 미리보기와 함께 본다."
    )


def _note_for_label(label: str) -> dict[str, str]:
    if label in EXPERIMENT_NOTES:
        return EXPERIMENT_NOTES[label]
    if "instruct" in label:
        return EXPERIMENT_NOTES["exp1-instruct-default-k20"]
    if "topk50" in label or "k50" in label:
        return EXPERIMENT_NOTES["exp2-topk50"]
    if "rrf" in label:
        return EXPERIMENT_NOTES["exp3-rrf-k20"]
    if "rerank" in label:
        return EXPERIMENT_NOTES["exp4-rerank-k20"]
    return EXPERIMENT_NOTES["baseline-raw-k20"]
```

- [ ] **Step 2: Run the focused tests**

Run:

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run pytest tests/eval/test_rag_quality.py -q
```

Expected:

```text
5 passed
```

- [ ] **Step 3: Commit the utility module**

Run:

```bash
git add src/eddr/eval/__init__.py src/eddr/eval/rag_quality.py tests/eval/test_rag_quality.py
git commit -m "feat: add rag quality report utilities"
```

Expected: commit succeeds with three tracked files.

---

### Task 4: Add Report-Building CLI

**Files:**
- Create: `scripts/build_rag_quality_report.py`
- Test: `tests/eval/test_rag_quality.py`

- [ ] **Step 1: Write the script**

Create `scripts/build_rag_quality_report.py`:

```python
"""Build the EDDR RAG quality assignment report from benchmark artifacts."""

from __future__ import annotations

import argparse
from pathlib import Path

from eddr.eval.rag_quality import (
    load_experiment_records,
    load_question_specs,
    render_assignment_report,
)


def main() -> None:
    """CLI entry point."""
    parser = argparse.ArgumentParser(description="Build RAG quality assignment report")
    parser.add_argument("--questions", type=Path, default=Path("docs/rag_quality/questions.yaml"))
    parser.add_argument(
        "--experiments",
        type=Path,
        default=Path("reports/rag_quality/retrieval/experiments.jsonl"),
    )
    parser.add_argument("--baseline-label", default="baseline-raw-k20")
    parser.add_argument("--golden-report", type=Path, default=None)
    parser.add_argument("--out", type=Path, default=Path("reports/rag_quality/ASSIGNMENT_REPORT.md"))
    args = parser.parse_args()

    questions = load_question_specs(args.questions)
    records = load_experiment_records(args.experiments)
    report = render_assignment_report(
        questions=questions,
        records=records,
        baseline_label=args.baseline_label,
        golden_report_path=args.golden_report,
    )
    args.out.parent.mkdir(parents=True, exist_ok=True)
    args.out.write_text(report, encoding="utf-8")
    print(f"wrote {args.out}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Run the script against a temporary sample ledger**

Run:

```bash
mkdir -p /private/tmp/eddr-rag-quality-sample
python - <<'PY'
import json
from pathlib import Path
out = Path('/private/tmp/eddr-rag-quality-sample/experiments.jsonl')
records = [
    {
        "ts": "2026-06-14 16:00:00",
        "git_rev": "abc1234",
        "label": "baseline-raw-k20",
        "config": {"k": 20, "rrf": False, "rerank": False},
        "questions": [],
        "aggregate": {"mean_recall_norm": 0.5, "mean_diag_recall@500": 0.7, "total_latency_s": 4.2},
    },
    {
        "ts": "2026-06-14 16:10:00",
        "git_rev": "abc1234",
        "label": "exp1-instruct-default-k20",
        "config": {"k": 20, "rrf": False, "rerank": False},
        "questions": [],
        "aggregate": {"mean_recall_norm": 0.65, "mean_diag_recall@500": 0.8, "total_latency_s": 4.5},
    },
]
out.write_text("\n".join(json.dumps(r, ensure_ascii=False) for r in records) + "\n", encoding="utf-8")
PY
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run python scripts/build_rag_quality_report.py --experiments /private/tmp/eddr-rag-quality-sample/experiments.jsonl --out /private/tmp/eddr-rag-quality-sample/ASSIGNMENT_REPORT.md
```

Expected output:

```text
wrote /private/tmp/eddr-rag-quality-sample/ASSIGNMENT_REPORT.md
```

- [ ] **Step 3: Inspect the generated sample report**

Run:

```bash
rg -n "평가용 질문셋|Baseline RAG 설정|성능 개선 실험|결과 분석" /private/tmp/eddr-rag-quality-sample/ASSIGNMENT_REPORT.md
```

Expected output contains these four headings:

```text
## 1. 평가용 질문셋
## 2. Baseline RAG 설정
## 3. 성능 개선 실험
## 4. 결과 분석
```

- [ ] **Step 4: Run focused tests again**

Run:

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run pytest tests/eval/test_rag_quality.py -q
```

Expected:

```text
5 passed
```

- [ ] **Step 5: Commit the script**

Run:

```bash
git add scripts/build_rag_quality_report.py
git commit -m "feat: add rag quality report builder"
```

Expected: commit succeeds with one tracked script.

---

### Task 5: Run Retrieval Experiments

**Files:**
- Runtime create/modify: `reports/rag_quality/retrieval/experiments.jsonl`
- Runtime create/modify: `reports/rag_quality/retrieval/RESULTS.md`

- [ ] **Step 1: Confirm ground-truth counts before measuring**

Run:

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run python scripts/bench_retrieval.py --out reports/rag_quality/retrieval --gt-only
```

Expected: every line begins with `GT` and ends with `ok`, followed by:

```text
GT check passed — 측정은 생략(--gt-only).
```

- [ ] **Step 2: Run baseline raw-query k=20**

Run:

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run python scripts/bench_retrieval.py --out reports/rag_quality/retrieval --label baseline-raw-k20 --instruct none --k 20
```

Expected output includes:

```text
label=baseline-raw-k20
appended → reports/rag_quality/retrieval/experiments.jsonl
```

- [ ] **Step 3: Run experiment 1, query instruction**

Run:

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run python scripts/bench_retrieval.py --out reports/rag_quality/retrieval --label exp1-instruct-default-k20 --k 20
```

Expected output includes:

```text
label=exp1-instruct-default-k20
appended → reports/rag_quality/retrieval/experiments.jsonl
```

- [ ] **Step 4: Run experiment 2, top-k 50**

Run:

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run python scripts/bench_retrieval.py --out reports/rag_quality/retrieval --label exp2-topk50 --k 50
```

Expected output includes:

```text
label=exp2-topk50
appended → reports/rag_quality/retrieval/experiments.jsonl
```

- [ ] **Step 5: Run experiment 3, keyword RRF**

Run:

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run python scripts/bench_retrieval.py --out reports/rag_quality/retrieval --label exp3-rrf-k20 --k 20 --rrf
```

Expected output includes:

```text
label=exp3-rrf-k20
appended → reports/rag_quality/retrieval/experiments.jsonl
```

- [ ] **Step 6: Verify that four records exist**

Run:

```bash
python - <<'PY'
from pathlib import Path
path = Path("reports/rag_quality/retrieval/experiments.jsonl")
labels = [line.split('"label": "', 1)[1].split('"', 1)[0] for line in path.read_text(encoding="utf-8").splitlines() if line.strip()]
print(labels)
assert labels == [
    "baseline-raw-k20",
    "exp1-instruct-default-k20",
    "exp2-topk50",
    "exp3-rrf-k20",
]
PY
```

Expected output:

```text
['baseline-raw-k20', 'exp1-instruct-default-k20', 'exp2-topk50', 'exp3-rrf-k20']
```

- [ ] **Step 7: Commit retrieval experiment artifacts**

Run:

```bash
git add reports/rag_quality/retrieval/experiments.jsonl reports/rag_quality/retrieval/RESULTS.md
git commit -m "test: record rag quality retrieval experiments"
```

Expected: commit succeeds with the retrieval report artifacts.

---

### Task 6: Run End-To-End Golden Search Check

**Files:**
- Runtime create: `reports/rag_quality/golden/*.md`

- [ ] **Step 1: Run `eddr golden` with the assignment question set**

Run:

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run eddr golden --golden-set docs/rag_quality/questions.yaml --out reports/rag_quality/golden
```

Expected output prints one progress line per question, such as:

```text
[G01] PASS
[G10] PASS
```

The exact PASS/FAIL counts may vary with local model/search state. Failures are acceptable evidence for the analysis section; command failure is not acceptable unless Ollama is down.

- [ ] **Step 2: Capture the newest generated Markdown path**

Run:

```bash
ls -t reports/rag_quality/golden/*.md | head -1
```

Expected: one path like:

```text
reports/rag_quality/golden/20260614_1630_v2_search.md
```

- [ ] **Step 3: Inspect the report structure**

Run:

```bash
latest="$(ls -t reports/rag_quality/golden/*.md | head -1)"
rg -n "PASS|FAIL|보류|추출|상위 lane|상위 사진" "$latest"
```

Expected: output includes summary rows plus per-question extraction/lane/photo preview lines.

- [ ] **Step 4: Commit the golden report**

Run:

```bash
git add reports/rag_quality/golden
git commit -m "test: record rag quality golden search run"
```

Expected: commit succeeds with the generated golden report.

---

### Task 7: Build Final Assignment Report

**Files:**
- Runtime create: `reports/rag_quality/ASSIGNMENT_REPORT.md`
- Read: `reports/rag_quality/retrieval/experiments.jsonl`
- Read: latest `reports/rag_quality/golden/*.md`

- [ ] **Step 1: Build the report**

Run:

```bash
latest="$(ls -t reports/rag_quality/golden/*.md | head -1)"
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run python scripts/build_rag_quality_report.py --golden-report "$latest"
```

Expected output:

```text
wrote reports/rag_quality/ASSIGNMENT_REPORT.md
```

- [ ] **Step 2: Verify required assignment sections**

Run:

```bash
rg -n "평가용 질문셋|Baseline RAG 설정|성능 개선 실험|결과 분석|검증 방법" reports/rag_quality/ASSIGNMENT_REPORT.md
```

Expected output includes:

```text
## 1. 평가용 질문셋
## 2. Baseline RAG 설정
## 3. 성능 개선 실험
## 4. 결과 분석
## 5. 검증 방법
```

- [ ] **Step 3: Verify the report mentions all experiment labels**

Run:

```bash
rg -n "baseline-raw-k20|exp1-instruct-default-k20|exp2-topk50|exp3-rrf-k20" reports/rag_quality/ASSIGNMENT_REPORT.md
```

Expected: each label appears at least once.

- [ ] **Step 4: Commit the final report**

Run:

```bash
git add reports/rag_quality/ASSIGNMENT_REPORT.md
git commit -m "docs: add rag quality assignment report"
```

Expected: commit succeeds with the final report.

---

### Task 8: Final Verification

**Files:**
- Verify only

- [ ] **Step 1: Run focused Python tests**

Run:

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run pytest tests/eval/test_rag_quality.py tests/query/test_golden.py tests/query/test_tools.py tests/server/test_search.py -q
```

Expected: all selected tests pass.

- [ ] **Step 2: Run the full non-long-running pytest suite**

Run:

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-eddr uv run pytest -q
```

Expected: all tests pass. If this fails due to an unrelated existing dirty-worktree issue, capture the exact failing test and error text in the final handoff.

- [ ] **Step 3: Check report artifacts**

Run:

```bash
test -s docs/rag_quality/questions.yaml
test -s reports/rag_quality/retrieval/experiments.jsonl
test -s reports/rag_quality/retrieval/RESULTS.md
test -s reports/rag_quality/ASSIGNMENT_REPORT.md
echo "rag quality artifacts present"
```

Expected:

```text
rag quality artifacts present
```

- [ ] **Step 4: Inspect git status**

Run:

```bash
git status --short
```

Expected: only pre-existing unrelated dirty files remain. In this session those were already present before the plan work: `AGENTS.md`, `CLAUDE.md`, `.codex/`, `docs/.golden_set.yaml.swp`, and `docs/critics/`. Do not stage or revert them unless the user explicitly asks.

## Self-Review

- **Spec coverage:** Task 1 covers the 10-question set and difficulty diversity. Task 5 covers at least two improvement experiments, plus a third. Task 7 renders baseline settings and result analysis. Task 6 adds end-to-end search-path evidence.
- **Placeholder scan:** This plan has no placeholder tokens, no deferred implementation notes, and no unspecified code step.
- **Type consistency:** The test imports match the implementation names: `QuestionSpec`, `ExperimentRecord`, `load_question_specs`, `load_experiment_records`, `summarize_experiments`, and `render_assignment_report`.

## Execution Choice

Plan complete and saved to `docs/superpowers/plans/2026-06-14-rag-quality-evaluation.md`. Two execution options:

1. **Subagent-Driven (recommended)** - Dispatch a fresh subagent per task, review between tasks, fast iteration.
2. **Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints.

Which approach?

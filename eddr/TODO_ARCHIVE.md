# EDDR TODO Archive

> 완료된 TODO 항목 보관소. **활성 할 일은 [`TODO.md`](TODO.md)**. 이관 규약: [`AGENTS.md` §7 ARCHIVE](AGENTS.md).
> 각 항목: **완료 날짜·시각(분단위) + 대표 git commit hash**. 최신순.

## 2026-06-15

- [x] **(RAG 품질) G08 날짜질의 lane 재정렬 — 골든 10/10 달성** — fact 질의(시간 의문사 `is_date_intent`) 감지 → `group_by_kst_date(order_by_date=True)` 날짜 오름차순(trip 시작일 top). route·golden 러너 배선, `run_search`·추출·프론트 계약 불변. TDD 4 task(분류기·정렬·러너·게이트). **골든 게이트 실측: G08 FAIL→PASS·G04 PASS 유지·photo_list 회귀 0 → 10/10**(report `reports/golden/20260615_1651_v2_search.md`). 브레인스토밍→spec→plan: `docs/superpowers/`. — 완료 **2026-06-15 16:55** · commit `f453b0b`·`25840ef`·`886b511`
- [x] **(코드정리) index_errors prune 파이프라인 자동호출** — vision run·recaption·geocode run 성공부에서 `db.prune_index_errors()` 호출(self-healing·출력 `pruned_errors=N`)로 재누적 방지. 테스트 2건(geocode·vision run 후 해소 에러 정리). — 완료 **2026-06-15 15:20** · commit `a2333f8`
- [x] **(코드정리) ruff format 부채 4파일 정규화** — 편집 게이트 미적용 과거 파일(prompt_ab_eval·test_caption_audit_cli·test_rag_quality·test_prompt_ab) format → 트리 전체 107파일 clean. — 완료 **2026-06-15 15:17** · commit `2fd33bf`
- [x] **(코드정리) index_errors 잔존 기록 정리** — `prune_index_errors()`(vision=캡션·geocode=country reconcile, 멱등 삭제) + `eddr db prune-errors` CLI. **실DB 적용: index_errors 1,248 → 262**(986 stale 삭제, 캡션 없는 진짜 미해소 262 보존). TDD 2건(단위·CLI). 파이프라인 자동 호출은 보류(§🧹 backlog). — 완료 **2026-06-15 13:51** · commit `e601ba4`
- [x] **(코드정리) caption N+1 제거** — `get_latest_captions_for_ids`(ROW_NUMBER 배치) 신설, `_rerank`·`search_photos`·`semantic_search_photos`·`get_trip`·`semantic_search` 호출부 배치화, `_photo_summary`는 caption 주입. TDD 등가성 + N+1 회귀 가드. 로컬 SQLite라 체감 이득은 제한적이나 N+1 패턴 제거. — 완료 **2026-06-15 13:46** · commit `783e0cb`
- [x] **(코드정리) eddr golden CLI 테스트** — `_cmd_golden` 단위테스트(정상 PASS 집계·ConnectionError 경로) 신설, CLI 커버리지 갭 해소(로직층은 test_golden.py 기존). — 완료 **2026-06-15 09:37** · commit `3cd2463`
- [x] **(코드정리) 모델명 상수화 + 테스트 LAN IP 플레이스홀더** — `src/eddr/constants.py` 신설(EXTRACT/CAPTION/EMBEDDING/DOC_RECAPTION/NONDOC_RECAPTION, 값 불변, doc=qwen3-vl·nondoc=gemma4:31b는 batch.py 정합), 정의처·테스트 import 치환, 개인 LAN IP → `FAKE_OLLAMA_HOST`. — 완료 **2026-06-15 09:33** · commit `58358dc`
- [x] **(정리) 문서·저장소 정합화 + graphify 코드 재인덱싱** — CLAUDE.md/TODO를 D26 현실로 교정(외부 LLM 0회·M3~M5 구현완료·캡션 1,393 재캡션), 미커밋 housekeeping 커밋(AGENTS/CLAUDE 규칙·docs/critics·.codex), `.gitignore`(graphify-out·swp), graphify 코드그래프 재생성(20→2,612 노드). — 완료 **2026-06-15 09:21** · commit `8f7ecdc`·`1b35e39`
- [x] **RAG 품질 평가·개선 통합 리포트** — 4요건(질문셋 10문항·baseline·개선실험 3종·결과분석) 충족. retrieval microbench(k-정규화 recall, GT=gemma4:e2b 고정): baseline **0.644 → query embedding instruction 0.732**(+0.088, production 기본 채택)·top-k50 0.749(coverage 효과)·RRF 0.718(broad keyword 잡음 → 기각 유지). E2E 골든 **9 PASS / 1 FAIL**(G08 날짜질의 → RAG 품질 트랙으로 이월). 재캡션의 RAG 효과는 GT=e2b 고정이라 미수치화(후속 과제). → [리포트](reports/rag_quality/ASSIGNMENT_REPORT.md) — 완료 **2026-06-15 07:14** · commit `bb74746`(외 `aac9ce1`·`2a3caf8`·`dec5465`·`d9dd9f4`·`82d4f9a`·`54aeb56`·`a80e617`·`4e39d0e`)

## 2026-06-14

- [x] **캡션 품질 audit + 투트랙 재캡션 (음식 strand)** — 콩나물→noodle 등 오캡션 해소. search audit·prompt-ab-eval 인프라(`e1233ab`) + 도메인 라우팅 듀얼 캡션(`daa05f9`·`32cabf1`) + p4/p5_grounded 프롬프트(`0ced224`·`061e94b`). 실측 재캡션 **음식 1,393장**(gemma4:31b 995 + qwen3-vl:8b 398, DB 검증: caption distinct 9,383·재캡션 1,393), **재임베딩 100% 정합**. ChromaDB 멀티스레드 데드락 → 캡션생성/Chroma색인 스레드 분리(`88b5d1e` `--no-vector`+`reindex-vectors`)로 회피, mlx 비전 환각 → GGUF `gemma4:31b` 롤백, ollama 무한 hang → timeout 600s(`d0e394c`). → 포스트모템 [`caption-recaption`](docs/postmortem/2026-06-14-caption-recaption.md)·[`naengmyeon`](docs/postmortem/2026-06-14-naengmyeon-food-caption-quality.md) — 완료 **2026-06-14 20:54** · commit `292c588`(merge)·`8383f7e`·`f3d705e`
- [x] **UX Quick Wins 4종 (제3자 UX 평가 기반)** — U1 빈상태 온보딩+예시질의 칩(`64eee1d`)·U2 해석칩 한국어화+길게눌러 영어 토글(`d9688aa`·`d79c52c`)·U4 위치미상 드로어 가치순(trip·장수) 정렬(`c441ea8`)·U6 상태배지→상태시트(`42a1506`). 근거 `docs/critics/2026-06-14_third-party-ux-eval.md`. — 완료 **2026-06-14 20:32** · commit `81f5f88`(merge)

## 2026-06-13

- [x] **내부 경계 dict/튜플 → frozen dataclass + ruff 규칙 확장** — repository 산출물 object화(`b343f8e` 7건·`640cb62` SearchLane·LanePhoto·GoldenRow·TopPhoto) + ruff 확장(N·C4·SIM·RUF·D-google) 전면 통과(`3bf8d8b`, PEP8·한국어 docstring 정비). — 완료 **2026-06-13 01:33** · commit `b343f8e`·`640cb62`·`3bf8d8b`

## 2026-06-12

- [x] **D26 M2 — MapLibre 지도 셸 + Tailscale HTTPS (폰 검증 PASS)** — 서버 `ee8301e`(map/photos GeoJSON 5,587점 gzip 216KB 실측·by-date KST·detail 좌표 — PhotoSummary/Detail dataclass는 비변경 우회) + 웹 `7cc6289`(MapLibre+zustand, 클러스터 3레이어·고줌 썸네일 마커 ≤60·현위치 폴백·DateDetailSheet·ChatPane 삭제, 번들 gzip 353KB). **폰 피드백 3건 반영** `3964d0a`: 썸네일 immutable 캐시+1.5x prefetch(팬 후 추가 요청 0)·시트 드래그 dismiss(플릭 속도 판정)·클러스터 탭=줌인 제거+즉시 날짜 섹션 ClusterSheet. 품질 리뷰 이슈 1건(시트 인스턴스 재사용) `40b1135` selectionSeq key 수정. 리뷰 2단계(스펙 PASS 0위반·품질 승인). Tailscale serve HTTPS(`https://macbookpro.tail848f4e.ts.net`) — serve-api 127.0.0.1 유지, 폰 iPhone Safari 실검증 전 구간 PASS(사용자 확인). pytest 241 passed. 구 M2 게이트(채팅 골든셋)는 D26으로 대체된 상태 — 본 M2 게이트는 폰 지도 브라우즈 E2E. → [impl-log](wiki/impl-log/map-shell-m2.md) — 완료 **2026-06-12 10:09** · commit `ee8301e`·`7cc6289`·`3964d0a`·`40b1135`
- [x] **D26 M0 — 문서 개정 (사용자 리뷰 승인)** — ADR-0009·scenario v2·prd v2·PLAN·CONTEXT·wiki 연쇄 + TODO 이관(`024f4cb`·`877c29c`). 사용자 리뷰: "시각자료가 없는 현 상황에서는 문제없음"(2026-06-12) — 이후 마일스톤 보고에 스크린샷 동봉으로 보완 운영. — 완료 **2026-06-12 10:09** · commit `024f4cb`
- [x] **D26 M1 — taken_at KST 정규화** — 진단이 기존 추정 반박(photos_library **전량 +00:00 UTC** 저장 — 시간대 히스토그램으로 인스턴트 정확성 검증, local 806은 naive 벽시계), 사용자 확정 규칙(aware=인스턴트 보존 astimezone·naive=벽시계 +09:00 부여·NULL 불변·`taken_at_raw` 1회성 스냅샷·백업·trips recompute)대로 백필. 실DB: 달력일 변경 **1,743장**(photos_library 1,016·takeout 727 — 기대값 일치), 잔존 미부착 0, **Photos 앱 대조 5/5**, trips **83→82·배정 3,760→3,777**(local 인스턴트 9h 교정의 예상 효과). 로더 `_iso_or_none`에도 정규화 적용(재적재 drift 방지). 2단계 리뷰: 스펙 ✅ / 품질 리뷰 백업 이슈 → `Connection.backup()` 전환 중 **동일 연결 자기교착 발견·해결**(쓰기 트랜잭션 쥔 연결로 backup() 호출 시 BUSY 무한 재시도 행 — probe 잠금과 백업 연결 분리, faulthandler로 특정). suite **218 passed**. → [impl-log](wiki/impl-log/kst-normalization.md) — 완료 **2026-06-12 07:53** · commit `afa4090`(본체) · `51f4584`(백업 수정)

## 2026-06-11

- [x] **(결정) ⑧-ⓑ 2차 실 Claude API 채점 — 공식 폐기** — ADR-0009 §2: 채점 대상(ChatEngine)이 D26으로 소멸, anthropic 의존 자체가 제거됨(M3). 골든셋 회귀는 D26 M3의 검색 결과 자동 채점(v2, `eddr golden` → `POST /api/search`)이 대체. 보류 결정(2026-06-11 20시 "제품 완성 후 연기")을 같은 날 D26 확정이 폐기로 전환. — 완료 **2026-06-11 23:40** · commit `024f4cb`
- [x] **(결정) ① 하위: ADR-0003 persons 명세 정리** — ADR-0009가 ADR-0003 자체를 supersede하여 종결 — tool surface 개념 소멸, `QueryService`는 내부 검색 서비스로 존속(amend 불요, persons 파라미터 명세 불일치 무의미화). wiki tool-surface.md는 archived 표기. — 완료 **2026-06-11 23:40** · commit `024f4cb`
- [x] **(결정) ① 하위: 날짜 필터 타임존 정책** — **D26-⑧로 확정**: taken_at을 KST(+09:00) aware ISO로 정규화 백필, "하루"=KST 달력일(ADR-0009 §6). 소스별 혼재(photos_library 촬영지 offset aware · takeout UTC aware · local naive)는 진단 실측 후 변환 규칙 확정(사용자 확인 1회) — 구현은 D26 M1. 질의 경계 KST→UTC 변환안은 기각(데이터 단일화가 lane UX의 전제). — 완료 **2026-06-11 23:40** · commit `024f4cb`
- [x] **D25 M2 게이트(폰 골든셋 9/10)·M3(trips/enrichment/SSE)·M4(원격 접근) — D26으로 supersede** — 실사용 판정("채팅 형식이 불편")으로 채팅 UX 폐기 → 채팅 경로 게이트 무의미화. M1·M2 구현 자산(서버 인프라 ADR-0008·썸네일·라이트박스·SPA 골격)은 D26이 계승, Tailscale(구 M4)은 D26 M2로 흡수, 확정 ③(SSE)·④(대화 영속화)는 무효, 확정 ⑤(Gradio 삭제)는 D26 M3에서 이행. — 완료 **2026-06-11 23:40** · commit `024f4cb`
- [x] **🙋 D24 사용자 문답 보강(enrichment) 백로그 — D26으로 흡수** — 목적(G06류 데이터 부재 해소)은 신 S4 수동 지오코딩(M4: GPS 무 2,867장/521그룹 일별 드로어 → Nominatim/long-press 위치 지정) + S5 사진 메모(M5: 한국어 자유 텍스트 + 임베딩 합류)가 구현 형태로 계승. 빌드 타임 wizard(`eddr enrich`)·채팅 중 문답(v1/v2 항목)은 미구현 상태로 폐기. — 완료 **2026-06-11 23:40** · commit `024f4cb`
- [x] **D25 M1 API 서버 + ADR-0008** — ADR `f605b86` + feat `f00539d`(15 files·+646). **ADR-0008 3계약**: ① server 위치 `src/eddr/server/`(기존 패키지 안, 단일 pyproject) ② photo_id 간접 서빙 — URL에 경로 미수용·응답에 절대경로 미노출(ADR-0001 웹 확장)·상대 image_path 9,959건은 `EDDR_ROOT`(`--root` > env > CWD) 기준 resolve, db/chroma/캐시 기본 경로도 root 파생 ③ 무인증 가드 — 기본 127.0.0.1·LAN 명시 플래그+기동 경고·공개 인터넷 직노출 금지. **구현**: `eddr serve-api`(기본 :8000) — `POST /api/chat`(전역 asyncio.Lock 선검사, 동시 2턴 **409 busy**·엔진 오류 502 원인 전달·threadpool wrap)·`/api/chat/reset`(204)·`GET /api/photos/{id}`(get_photo 스키마 동형)·`/thumb?size=320|1280`(화이트리스트 외 422, 전 포맷 JPEG q85, `{photo_id}_{size}.jpg` 캐시·single-flight·tmp→rename·exif_transpose)·`/api/status`(ready/total + `indexing_stage_counts` 신규(repository) + path_health 표본 20)·`/api/healthz`. deps: fastapi·uvicorn(본)+httpx(dev) — `uv lock`+`uv pip install`(sync 회피). 테스트 17건 신규(tests/server/) suite 165→**201 passed**. **수용 PASS 7/7**(sonnet 판정): /tmp에서 `EDDR_ROOT`만으로 기동(healthz **1s**)·status ready=total=**9,218**·**path_health 20/20 healthy**(M1 게이트)·실HEIC→320px JPEG 37.8KB(캐시 히트 **2.5ms**, NFR p95<200ms 충족)·detail 좌표/경로 미노출·**curl G08 재현**: "2019-06-29~07-12 이탈리아·278장" 정답(ollama qwen3.6:27b, 2m18s). 관찰 2건(회귀 아님): G08 photo_ids 빈 배열(list_trips는 설계상 사진 id 미반환 — Gradio 동일), ollama 답변 "2박 3일(약 12일)" 자기모순(백엔드 품질 — 본선 Claude 무관). → [impl-log](wiki/impl-log/web-server-m1.md) — 완료 **2026-06-11 20:03** · commit `f605b86`(ADR) · `f00539d`(feat)
- [x] **(코드품질) image_path 표기 통일/CWD 의존** — D25 M1 `EDDR_ROOT` resolve 계약(`server/deps.py:resolve_image_path`)으로 해소: 절대경로(local 1,730)는 그대로, 상대(photos_library 8,574·takeout 1,385)는 root 기준 — path_health가 오설정 상시 감시. 잔여인 Gradio `eddr serve`의 CWD 의존은 M2의 app.py 삭제로 소멸 예정. — 완료 **2026-06-11 20:03** · commit `f00539d`
- [x] **(사용자) D25 scenario.md·prd.md 리뷰 — 미세 결정 ①~⑥ 확정** — 압축 표기가 모바일에서 파악 어렵다는 피드백으로 6건을 풀어서 재질의(AskUserQuestion), 전부 추천안 채택: ① phase 단일 체계 ② 레이아웃 A안(`src/eddr/server/`+루트 `web/`) ③ SSE M3 ④ 대화 영속화 M3 재결정 ⑤ Gradio M2 완료 시 삭제 ⑥ ADR-0008 M1 착수 직전 작성. prd.md `[확정 n]` 표기로 반영(동시각 커밋). — 완료 **2026-06-11 19:32** · 기획 신설 commit `c698520`
- [x] **① 하위: R2(누구랑) 3문항 재정의** — 사용자 골든셋 선별(2026-06-11 모바일 회신: Q1·4·5·9·10·25·31 + 신규 3문항)이 person 질의 **0문항**으로 분포를 확정(R1 2 / R3 7 / 복합 1) — ADR-0004 D10(person 질의 폐기)과 정합해 별도 재정의 불요로 해소. 기존 "R1 5/R2 3/R3 2" 분포 계획 대체. 골든셋 본체(`docs/golden_set.yaml`)는 `confirmed: pending`으로 TODO ① 잔존. — 완료 **2026-06-11 09:02** · commit `1396bde`
- [x] **⑦ Gradio UI + Claude API + 5 tools — 서비스 완성** — 테스트 21건 추가(suite 144→**165 passed**), 커밋 2분할. **5 tools**(`query/tools.py`, ADR-0003): `search_photos`·`semantic_search_photos`·`list_trips`·`get_trip`·`get_photo` — 응답 dataclass에 좌표·PII·image_path **필드 자체 부재**로 ADR-0001 스키마 강제(테스트가 `fields()`로 고정), limit 1~50 클램프. **설계 입력 3건 반영**: ① 캡션 `Search keywords:` bold(4,370)/plain(5,013) 모두 파싱(`query/captions.py`) ② GPS 무 사진 분리 — `(country IS NULL)` 하단 정렬 + `has_location` 필드 + 프롬프트 지시 ③ dedup 필터 — `_photo_filter_sql()`이 전 질의 경로에 `duplicate_of IS NULL`·영상 제외 주입, get_photo는 duplicate→canonical. **+rank 기반 거리**(§D-4): semantic 응답 rank 1차 신호·distance 참고용, 컷오프 금지를 tool description·프롬프트 양쪽 명시. **semantic 합성**(§D-5 확증 구현): Chroma 메타에 geocode·날짜 없음 → k×5 over-fetch 후 SQL 후처리(`filter_photo_ids`, 거리순 보존) — 실측 "바다"+대한민국+2014↑ → 인천·서귀포. **챗 엔진**(`query/engine.py`): anthropic manual tool use loop(최대 8왕복)·adaptive thinking·system 캐싱(`cache_control`, 인덱싱 N/M·날짜는 엔진 생성 시 고정), 한국어 프롬프트에 도구 분기(무엇=semantic/지명·시점=메타, findings §8.6)·tool 오류 `is_error` 회송, 기본 `claude-opus-4-8`(`--model` 오버라이드), photo_id만 side-channel 수집(이미지 경로 LLM 미전송). **Gradio**(`query/app.py`, gr 6.17): Blocks 채팅+Gallery 5열+인덱싱 footer, **HEIC→JPEG 썸네일**(pillow-heif, `data/cache/thumbs` sha1 캐시 — photos_library HEIC 다수라 핵심 경로), API 키 부재 챗 버블 안내; gr 6 호환 2건 수정(`Chatbot type=`·`launch show_api=` 제거됨). **실DB 검증**: indexing_stats **9,218/9,218**, R1 즉답 재현(몽골 2018-07 448장 CN·MN / 이탈리아 2019-06 278장 IT), semantic "오로라" top-1·2 아이슬란드 적중, `eddr serve` HTTP 200(4s)+Playwright E2E(입력→응답 표시) PASS. **잔여**: 실 API 호출 검증(키 미설정) → ⑧에서. 발견: image_path 상대경로 실측 정정(절대는 local뿐 — TODO 갱신). → [impl-log](wiki/impl-log/query-service.md) — 완료 **2026-06-11 07:36** · commit `22b09f4`(데이터 레이어) · `15ec542`(엔진+UI+serve)
- [x] **⑦ 거리 임계값 설계 주의(코드품질)** — L2 절대값 컷오프 불가(§D-4) → ⑦ 구현에 rank 기반으로 반영 완료(위 항목). — 완료 **2026-06-11 07:36** · commit `22b09f4`
- [x] **⑥ Trip 클러스터링** — TDD 27테스트 추가(suite 115→142 passed), 커밋 3분할. **선행 결정 2건(사용자)**: trip_countries 값은 한국어명·하드코딩 매핑 대신 **ISO 코드 백필**, 영상(skipped_video 2,306, GPS 2,301 포함)은 세그먼트 입력·배정 **완전 제외**. **geocode 확장**: Nominatim 파서가 ISO `country_code`(대문자) 보존 + geocode_cache 멱등 컬럼 추가 + `eddr geocode backfill-country-code` 재조회 백필 — **2,047/2,047셀·오류 0·약 35분·9개국**(KR 1,674·IS 181·IT 91·MN 54·TH 27·CN 9·GB 5·DE 4·NL 2). **세그멘테이션**(`trips/cluster.py` stdlib): 일상영역(wizard 4건) in/out → out 연속 run(분리 주신호=**복귀**, 안전장치 사진공백 **72h** — 24h는 "여행 중 하루 무촬영" 가짜 분리, TDD 적발) → 스팬 24h+ 필터(D14). **파이프라인**(`eddr trips recompute`, 전체 재계산 멱등): 결정적 id `trip_YYYYMMDD_NN`·자동 이름(해외=최빈 외국국가·국내=최빈 city)·기간 배정(no-GPS 포함, PLAN §8)·`caption_done→trip_assigned` 전이만·taken_at aware/naive 혼재는 SQLite `datetime()` 정규화로 흡수. **실DB**: **83 trips·배정 3,760**(no-GPS 758 — EDA no-GPS 773 과제 1차 해소; 이탈리아 사본 dup 160은 taken_at NULL 자연 제외), 기간 최단 1.01·평균 2.8·최장 14.7일, 0.7초 완주·재실행 동일. **trip_countries 거주국 제외**(실DB 검증서 CONTEXT.md "인천→로마→뮌헨→인천=2개국" 위반 발견·교정): 아이슬란드 2022-09(530장, DE·GB·IS)·몽골 2018-07(448, CN·MN)·이탈리아 2019-06(278, IT — **EDA 최장 trip과 경계 일치**). 44→83은 반경 축소(50km→5~6.6km 4영역)의 예상 결과. **R1 기준 시나리오 즉답 가능**: 몽골 2018-07·이탈리아 2019-06. → [impl-log](wiki/impl-log/trip-clustering.md) — 완료 **2026-06-11 01:15** · commit `578323f`(geocode 경로) · `c022de1`(클러스터링) · `72e8db4`(거주국 제외+실행)
- [x] **(사용자) `eddr setup daily-radius` wizard 실행 — ④ 잔여 해소** — 일상 영역 **4건 확정**: 집/직장(강남, 6.6km)·집(강서, 5.4km)·집(김포, 5.1km)·서울특별시 강남구(성동/광진권, 5.2km). ⑥ Trip의 선행 조건 충족. — 완료 **2026-06-11 00:11** · DB 전용(daily_radius_areas, 코드 변경 없음)

## 2026-06-10

- [x] **④ dedup + geocode + Daily Radius + setup wizard** — TDD 51테스트 추가(suite 63→115 passed), 커밋 3분할. **dedup**: BLAKE3(takeout staging 동일 포맷 교차검증)+dHash(imagehash·EDA 포맷, HEIC=pillow-heif) 백필 `eddr dedup backfill-hashes` — **7,653장**(photos_library 6,268 + takeout 1,385) 오류 0, RAW 65장만 dHash 불가; cross-source 마킹 `eddr dedup mark`(전체 재계산 멱등, photos_library>local>google_takeout, 같은 소스 형제 미마킹=ADR-0002) — **165그룹·165건**(local 160 + takeout 5, canonical 전부 photos_library), **마킹 local 160 중 155건 = `2019_이탈리아` 폴더**로 노트북 05 §D-5 사용자 육안 판정("이탈리아 top-5 iCloud 사본") 확증, 무결성(dangling·체인) 0. **geocode**: `eddr geocode run` — Nominatim(1req/s·UA·accept-language=ko·zoom=14) + 3dp 밀리도 geocode_cache, 셀 중심 요청·negative 캐시·연속 5실패 중단; 실측 **photos 7,888 전량 / 2,047셀(예측 일치) / 오류 0 / 약 35분**, country·city 100%·district 86%, 한국어 지명(서울특별시 강남구 730 = EDA 격자 피크 730 교차 일치), 국가 분포 한국 6,303·아이슬란드 1,051·이탈리아 261·몽골 175. **Daily Radius**: 0.01° 격자 greedy 병합(stdlib) + `eddr setup daily-radius` 대화형 wizard(라벨·반경 편집, 전체 교체 멱등, EOF 안전, --propose-only) — 후보 8건(강남 918·용산 557·강서 488·김포 417·레이캬비크 181…), 병합 총량≠피크 비단조 버그(정렬·min_count 조기 break) TDD 교정. **사용자 wizard 확정만 잔여**(TODO ④ 항목). → [impl-log](wiki/impl-log/dedup-geocode-radius.md) · [PLAN §4.2](docs/PLAN.md) — 완료 **2026-06-10 23:55** · commit `b4d8c6f`(스키마+dedup) · `029e320`(geocode) · `bda386a`(Daily Radius) + 문서·정렬픽스 커밋
- [x] **③ 인덱싱 1단계 (잔여 해소)** — 본체(소스 적재·D18 필터·upsert)는 선행 완료(`b5e230d` 등), 잔여였던 **trips·trip_countries·daily_radius_areas·geocode_cache 테이블 + photos 5컬럼**(country/city/district/trip_id ON DELETE SET NULL/duplicate_of)이 멱등 마이그레이션(베이스라인 DDL + PRAGMA 검사 ALTER, 운영 DB 사본 리허설로 11,689행·captions 9,383 보존 확인)으로 해소. enrichment 필드는 `upsert_photo` 불가침(재적재가 dedup·geocode 결과 리셋 안 함). PLAN §4.1 `duplicate_of` Source-first 반영. — 완료 **2026-06-10 22:10** · commit `b4d8c6f`

- [x] **(⑥ 선행) taken_at EXIF 콜론 포맷 정규화** — local 806건이 EXIF DateTimeOriginal 형식(`2018:04:10 18:38:51`)으로 저장돼 SQLite 날짜 함수에서 전량 누락되던 결함(노트북 05 §C-2 실측·상시 게이트 신설로 적발). TDD(RED 3건 먼저): `source_loader._iso_or_none`이 문자열을 `_normalize_exif_datetime`에 위임 — EXIF 패턴은 `strptime` 검증 후 ISO 변환, EXIF형 무효값(`0000:00:00…`)은 None, 그 외 원문 유지(suite **63 passed**). 사전 실측: 매니페스트 비-null exif_date 2,191건 전수 EXIF 패턴(무효값 0)·타 소스 ISO 경로 영향 없음. DB 교정은 `eddr db load-sources` 재적재(`taken_at = excluded.taken_at`) — local unparsable **806→0**, 상태(caption_done 9,383·skipped_video 2,306)·captions·embeddings 9,383 보존, local 연도 축(2015~2025) 복원. 노트북 05 재실행으로 **§E-1 12게이트 전원 PASS(ALL PASS) 복귀** 확인. 중간에 DB Browser 잠금(journal=delete) 충돌 — 사용자 해제 후 재개. — 완료 **2026-06-10 21:35** · commit `c4b2199`

- [x] **🚨 (⑤ 후속) load-sources 2건 수정 + 회귀 테스트** — TDD(소실 시나리오 RED 먼저): ① `upsert_photo` ON CONFLICT의 `indexing_status`를 CASE WHEN으로 보호 — vision 이후 단계(caption_done·trip_assigned·skipped_video)는 보존, 적재 단계(meta_done·missing_image)는 갱신; 재적재가 caption_done 9,383건을 meta_done으로 리셋해 재캡션 대상화하던 결함 제거, 상태 전이 5케이스 parametrize 고정. ② D18 영상 확장자 필터(`VIDEO_EXTENSIONS` 11종·`_is_video_path`, 대소문자 무시)를 3경로 공통 적용 — vision_manifest(yield None)·takeout(skipped 집계)·photos export stem 매핑(Live Photo `.mov` 오염 방어). 회귀 테스트 9건 추가, suite **57 passed**. 기존 `skipped_video` 2,306행은 의도 제외로 불변. **`eddr db load-sources` 재실행 금지 제약 해제.** — 완료 **2026-06-10 20:15** · commit `b5e230d`

- [x] **노트북 05 §D stale 해소 + 검증 전면 강화 재실행** — `notebooks/05_index_verification.ipynb`(gitignored)를 §A–§E로 확장: §B-5 캡션 본문 전수 일치(Chroma documents == captions.text, **9,383/9,383**) · §C 상세 검수(파일 실존 전수 9,383/9,383 · 소스별 필드 충전율 · 캡션 품질/keywords 100% · 4계층 레코드 추적 · 무작위 9장 **사진↔캡션 그리드**) · §D 검색 검증(한국어 5쿼리 top-5 **실사진 그리드** · self-retrieval **top-1 98%/top-5 100%** · 음성 대조) · §E 종합 **11게이트 ALL PASS** + §E-2 라이브 재검증(`0beb879`·`2a52ad3` 해소 3건 상시 감시 — 기존 "§D stale 재실행" 항목을 일회성 확인이 아닌 자동 감시로 대체). 실측 발견 2건 → TODO 등록(image_path 표기 혼재 · L2 거리 컷오프 불가). — 완료 **2026-06-10 20:14** · commit `2267109`

- [x] **② 프로젝트 스켈레톤 — 전체 완료(마지막 잔여 = ruff 설정)** — `pyproject.toml`·uv·pytest(`pythonpath=src`)·디렉터리·`eddr` CLI entrypoint는 Takeout 빌드+foundation DB에서 선행 완료, 잔여였던 **ruff 설정**(line-length 100, pyproject.toml:42~53)이 ruff 도입·전 소스 적용 리팩터로 완료. → [PLAN.md §7](docs/PLAN.md) — 완료 **2026-06-08 23:24** · commit `ecfa96f`
- [x] **⑤ Vision 캡션 파이프라인 — 실데이터 전량 완료 + dual-server LAN 분산** — caption(영어·P3_hybrid) **9,383장** + caption_text 임베딩(DB captions = embeddings = Chroma `eddr_caption_text_v1` 9,383 3-레이어 정합), 영상 2,306장 `skipped_video` 제외. **dual-server**: caption을 로컬 + 사용자 LAN 노드 `192.168.0.56`에 분산(`run_caption_text_batch_dual` = caption 2노드 thread 동시 / embed·persist 로컬 단일 writer로 벡터공간 일관성), 원격이 더 빠름 **~4.35s/장**(단일 17.5s 대비 4x), ~7h13min 무오류 완주. 핵심 수정(TDD 48/48): ① **HEIC/미지원포맷 → sips temp JPEG 변환**(`to_ollama_image`·`needs_conversion`; HEIC 4,813장이 `unknown format`으로 막혔던 것 해소) ② **영상 `skipped_video`**(load-sources D18 누락 우회) ③ `OllamaVisionClient(host=)` 원격 + `--remote-host` CLI. 검증: 한국어 시맨틱 검색 바다·음식·야경 **5/5 관련**(D19 multilingual 프로덕션 확증). 프롬프트 **hybrid 확정**(v2 위치환각, findings §8.10). → [ADR-0007](docs/adr/0007-lan-distributed-vision.md) · [PLAN.md §5·§7](docs/PLAN.md) — 완료 **2026-06-08 22:57** · commit `ecfa96f`(코드·테스트, ruff/타입 리팩터 동봉) + 문서 커밋(ADR-0007·wiki)

## 2026-06-07

- [x] **Vision 배치 + 프롬프트 A/B 파이프라인 (코드)** — Ollama caption/embed 클라이언트(+민감 메타데이터 누출검사)·`prompt`(P3_hybrid/v2 named 프롬프트·누출감지)·`prompt_ab`(프롬프트 A/B 러너, 소스별 stratified 샘플링 `pending_vision_photos_stratified` = `ROW_NUMBER() PARTITION BY source` 라운드로빈)·`run_caption_text_batch`(caption→embed→Chroma upsert)·`eddr vision run`/`prompt-ab` CLI. TDD: stratified 샘플링 RED→GREEN, vision 9/9 · suite 41 passed. **실데이터 실행(photos export·Ollama)은 ⑤ Vision에 잔존.** → [PLAN.md §5·§7](docs/PLAN.md) — 완료 **2026-06-07 17:59** · commit `7a326da`

## 2026-06-04

- [x] **Day-level 장소추정 캡션 보강 EDA (04)** — `notebooks/04_day_geocaption_eda.ipynb`(gitignore) → findings §9. 03 지명약점(제주·일산 0.00) 캡션경로 보완 검증. multi-image 스모크 PASS · 29그룹 26b coarse 지명(공격/보수). recall@10 base 0.6/공격 0.6/보수 0.5, **제주·일산 0.00 그대로**. 환각이 발목(부산→Jeju)·'부익부' → **v1 보류**, 지명은 D14 메타. spec·plan `2026-06-04-day-geocaption-eda`. — 완료 **2026-06-04 17:46** · commit `d36ebb1`

- [x] **Vision 캡션 프롬프트 EDA (03)** — `notebooks/03_vision_caption_eda.ipynb`(gitignore) → findings [§8](docs/01_eda_findings.md). 정성 144캡션(26b 정확/e2b 환각) → 정량(e2b P2 vs P3, 풀 400). **best=P3_hybrid** · **D19 PASS**(recall@10 0.70 > P2 0.60). 통찰: 캡션검색 지명약점(제주·일산 0)→GPS/trip 보완 정당화. macOS NFD 교정. spec·plan [`2026-06-03-vision-caption-eda`](docs/superpowers/specs/2026-06-03-vision-caption-eda-design.md). — 완료 **2026-06-04 00:47** · commit `cb0ed90`

- [x] **Google Takeout 실데이터 적재 (Task 7)** — `data/google_photos/raw/`(macOS 자동압축해제, 2011–2016) → `[2011,2017)` 필터 → **staged 1,385장** + `manifest.jsonl`(staged=manifest=RETURNED 정합). 사이드카 39%뿐이나 EXIF+파일명 날짜 폴백으로 **208장 복구**(no_date 317→109). 잔여 109 = EXIF 제거 카메라 원본(복구 불가). → [impl-log](wiki/impl-log/google-takeout-staging.md) — 완료 **2026-06-04 00:27** · commit `b9c6c93`(복구 픽스; 적재는 `ingest()` 직접 호출)

## 2026-06-03

- [x] **Google Takeout 적재 파이프라인 빌드 (ADR-0005, Task 1–6)** — 3번째 소스 `google_takeout`: `coverage`(C 측정)·`sidecar`·`walk`·`stage`·`ingest` 5모듈, 합성 fixture TDD **19테스트**. 맥 연도 히스토그램 측정 → **C=2017 확정**. 실데이터(Task 7)는 사용자 익스포트 대기. → [impl-log](wiki/impl-log/google-takeout-staging.md) — 완료 **2026-06-03 23:34** · commit `11c7646` (작업 `7d241fe`→`11c7646`)
- [x] **풀데이터셋 EDA (02)** — `notebooks/02_full_dataset_eda.ipynb`(gitignored) → 리포트 [`docs/01_eda_findings.md §7`](docs/01_eda_findings.md). iCloud 9,054 정합(overlap/icloud_new) · 실 near-dup율(D8) · 해상도 · 다음세션 핸드오프(매니페스트+썸네일) 확보. 계획 [`docs/superpowers/plans/2026-06-03-full-dataset-eda.md`](docs/superpowers/plans/2026-06-03-full-dataset-eda.md). — 완료 **2026-06-03 19:57** · commit `e415155` (작업 `28947a1`→`e415155`)
- [x] **D12 로컬파일 정합·픽셀 EDA** — 02에서 수행(정합성 · 실 near-dup · 해상도). iCloud on-demand 다운로드 부하만 미측정(사용자가 로컬 아카이브 직접 제공으로 무의미화). → [§7](docs/01_eda_findings.md) — 완료 **2026-06-03 19:47** · commit `0a70612`
- [x] **Second-brain 문서 레이어** — schema/source 규약(AGENTS.md) · CLAUDE.md 포인터화 · wiki 12페이지 INGEST + WIKI_INDEX. — 완료 **2026-06-03 17:15** · commit `bc052d7`,`cd273ee`

## 2026-05-31

- [x] **EDA (구현 전 가정 검증, 01)** — `notebooks/01_eda.ipynb`(gitignored) → 리포트 [`docs/01_eda_findings.md`](docs/01_eda_findings.md). 메타데이터 가정 5건 VALIDATED. — 완료 **2026-05-31 22:57** · commit `895e9d4`
- [x] **EDA 후속 결정 ADR-0004** — D8 near-dup 보류 · D10 person 질의 폐기. [`docs/adr/0004-eda-driven-scope-decisions.md`](docs/adr/0004-eda-driven-scope-decisions.md) — 완료 **2026-05-31 22:57** · commit `895e9d4`
- [x] **설계 문서 베이스라인** — PLAN · CONTEXT · ADR-0001~0003. — 완료 **2026-05-31 22:57** · commit `97249c2`
